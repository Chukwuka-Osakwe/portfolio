/** Contact route shown in the card column. Placeholder copy for now. */
export function Contact() {
  return (
    <div className="mx-auto mt-[12vh] flex w-full max-w-2xl flex-col items-center">
      <p className="text-center text-[clamp(1.5rem,5vw,2.5rem)] font-semibold tracking-tight text-balance text-accent">
        better call chuka!
      </p>
    </div>
  );
}
